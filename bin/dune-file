(install
 (files
  mopsa mopsa.bin
  (glob_files mopsa-*)
  (glob_files_rec mopsa-wrappers/**))
 (section bin))

(install
 (section share_root)
 (package mopsa)
 (files
  (mopsa-completion as bash_completion/completions/mopsa-completion)))
