#!/bin/bash
MOPSADIR="$(cd "$(dirname "$0")" && pwd -P)/.."

STUBSDIR=${MOPSADIR}/analyzer/stubs/c

CONFIG=-config=${MOPSADIR}/analyzer/configs/c.json
STUBS=
INCLUDES=-I=${STUBSDIR}/mopsa

${MOPSADIR}/bin/mopsa -lang=c ${CONFIG} ${INCLUDES} ${STUBS} $@
