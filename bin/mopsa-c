#!/bin/bash

MOPSADIR="$(cd "$(dirname "$0")" && pwd -P)/.."
STUBSDIR=${MOPSADIR}/analyzer/stubs/c

CONFIG=${MOPSADIR}/analyzer/configs/c.json

FLAGS=-I=${STUBSDIR}/mopsa
STUBS="${STUBSDIR}/libc/libc.c ${STUBSDIR}/mopsa/mopsa.c"

${MOPSADIR}/bin/mopsa -lang=c -config=${CONFIG} ${FLAGS} ${STUBS} $@
