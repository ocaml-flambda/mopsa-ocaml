#!/bin/bash
MOPSADIR="$(cd "$(dirname "$0")" && pwd -P)/.."

STUBSDIR=${MOPSADIR}/analyzer/stubs/c
STUBS=${STUBSDIR}/libc/string.c
MOPSAINC=-I=${STUBSDIR}/mopsa

CONFIG=${MOPSADIR}/analyzer/configs/c.json

${MOPSADIR}/bin/mopsa -lang=c -config=${CONFIG} ${MOPSAINC} ${STUBS} $@
