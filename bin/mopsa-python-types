#!/bin/bash
MOPSADIR="$(cd "$(dirname "$0")" && pwd -P)/.."
CONFIG=python/types.json

${MOPSADIR}/bin/mopsa -config=${CONFIG}  -stub=$MOPSADIR/share/stubs/python/ -widening-delay=1 -loop-unrolling=1 -lflow $@
# -widening-delay=1
#-stub ${MOPSADIR}/analyzer/stubs/python
