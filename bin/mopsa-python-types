#!/bin/bash
MOPSADIR="$(cd "$(dirname "$0")" && pwd -P)/.."
CONFIG=python/types.json

${MOPSADIR}/bin/mopsa -config=${CONFIG} -widening-delay=1 -stub=$MOPSADIR/share/stubs/python/ $@
#-stub ${MOPSADIR}/analyzer/stubs/python
