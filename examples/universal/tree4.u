tree t;
int n;
int i;
int x;
string s;
unit u;
u = mopsa_assume(n>=0);
i = 0;
t = Tree("Nil",{});
while (i < n) {
  t = Tree("Concat",{Tree(x-1), t});
  t = Tree("Concat",{Tree(x+1), t});
  i = i + 1;
 };
